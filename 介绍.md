# OpenClaw Memory Manager

> 让 OpenClaw 越来越懂你——主动沉淀记忆，而不是等你提醒它"记住XXX"。

## 它解决什么问题

[TencentDB Agent Memory](https://github.com/agentmemory/agentmemory) 帮 OpenClaw 保存了会话记录，但它有两个根本局限：

1. **不会主动沉淀**：它只管存，不会把会话里的关键信息提炼进 `USER.md` / `MEMORY.md` / `AGENTS.md`。你得自己告诉 agent "把这个记下来"，它才会去更新记忆文件。
2. **检索成本高**：当 agent 需要回忆之前的项目细节时，必须调用 embedding 模型、检索记忆库、读取大段原文——费时、费 token、费钱。

本项目补上了这块缺失：**每天自动读取会话记录，用 LLM 提炼关键信息，主动写入三个记忆文件**。

效果是：
- **日常会话中提到之前的项目** → agent 直接从 `MEMORY.md` 读几行就够了，不需要检索
- **需要详细回顾某个项目** → 才去翻记忆文件或会话记录
- **你不需要提醒 agent "记住XXX"** → 它自己会沉淀

### 成本：每天不到 1 毛钱

这个系统每月调用 LLM 约 35 次，以 DeepSeek-v4-flash 为例：

| | 月费 | 年费 |
|---|---|---|
| 有缓存（常态） | **2.1 元** | **25 元** |
| 无缓存（最坏） | **2.8 元** | **33 元** |

每天 7-9 分钱。换来的是 agent 每次对话都更懂你、不用反复解释背景、不用费 token 检索记忆。

**对比：** 如果没有这个系统，agent 每次需要回忆旧项目时都要调 embedding 检索 + 读大段原文，一次检索的 token 成本可能就超过这个系统一整天的费用。

## 它怎么工作

每天 22:30 自动执行，读取 TencentDB Agent Memory 产生的会话记录，用 LLM 提炼后主动更新记忆文件：

```
每天 22:30
  │
  ├─ chunking：按 agent 分区（main / echo / ...）
  │
  ├─ main agent（优先）：
  │   ├─ 每天 → 提炼当天会话 → 更新 USER.md / MEMORY.md / AGENTS.md
  │   ├─ 周日 → 额外汇总7天摘要 → 周级沉淀
  │   └─ 每月1日 → 额外汇总上月周摘要 → 月级沉淀
  │
  └─ 其他 agent（如 echo）：同上，独立记忆分区
```

每个 agent 有独立的记忆文件和摘要目录，互不干扰。main agent 优先处理，确保主记忆先进化完成。

越往上越精炼，像人的记忆一样——短期细节会模糊，但重要信息会沉淀成长期记忆。

## 架构

```
会话记录 (JSONL, TencentDB Agent Memory 产生)
    │
    ▼
┌─────────────┐    ┌─────────────────────────┐
│  Chunking    │───→│ LLM Provider            │
│ (分块脚本)   │    │ 提炼(1000/1500/2000字)   │
└─────────────┘    │ + 进化(USER/MEMORY/AGENTS)│
                    └─────────────────────────┘
                             │
                             ▼
                   每日/周/月摘要 + 文件进化
                             │
                             ▼
                    NotificationAgent → WinRT Toast
```

## 目录结构

```
openclaw-memory-manager/
├── README.md
├── LICENSE
├── .gitignore
├── config.env.example          # 环境变量配置模板
└── scripts/
    ├── common/
    │   ├── __init__.py
    │   └── utils.py            # 共享工具模块
    ├── daily/
    │   ├── main.py             # 每日任务
    │   └── convert_jsonl_to_md.py  # Chunking 脚本
    ├── weekly/
    │   └── main.py             # 每周任务
    ├── monthly/
    │   └── main.py             # 每月任务
    ├── run_all.py              # 统一编排器（计划任务入口）
    ├── notify_agent.py         # 通知代理（WinRT Toast）
    └── install_tasks.bat       # 一键注册计划任务
```

运行后自动产生：
```
memory-chunking/          # Chunking 输出
memory-abstracted-daily/  # 每日摘要
memory-abstracted-weekly/ # 每周摘要
memory-abstracted-monthly/# 每月摘要
run_log/                  # 运行日志 + 状态文件
```

## 安装

### 前置条件

- Windows 10/11
- Python 3.10+（需在 PATH 中，或知道安装路径）
- OpenClaw 已安装，且 TencentDB Agent Memory 插件已启用
- DeepSeek API Key（[申请地址](https://platform.deepseek.com/)）

### 方式一：安装器（推荐）

```bash
# 克隆项目
cd %USERPROFILE%\.openclaw
git clone https://github.com/你的用户名/openclaw-memory-manager.git
cd openclaw-memory-manager

# 启动安装器
python setup.py
```

浏览器自动打开安装界面，填写 API Key 和路径后一键安装。安装器会自动：
- 设置环境变量（用户级 + 注册表）
- 注册计划任务（每天 22:30）
- 配置 NotificationAgent 开机自启
- 启动 NotificationAgent

### 方式二：手动安装

**第一步：配置 API Key**

系统环境变量（推荐）：

```powershell
[System.Environment]::SetEnvironmentVariable(
    "MEMORY_MANAGER_DEEPSEEK_API_KEY",
    "sk-你的key",
    "User"
)
```

或者同时写入注册表（计划任务进程可能不继承环境变量）：

```powershell
Set-ItemProperty -Path "HKCU:\Environment" -Name "MEMORY_MANAGER_DEEPSEEK_API_KEY" -Value "sk-你的key"
```

设置后需重启终端或注销重新登录生效。

**第二步：安装 WinRT 依赖（通知功能）**

```bash
pip install winrt-windows.ui.notifications winrt-windows.data.xml.dom
```

> 如果不需要通知功能，可跳过此步。脚本会在发送通知时静默失败。

**第三步：注册计划任务**

```bash
cd openclaw-memory-manager\scripts
install_tasks.bat
```

- 普通用户运行：注册当前用户的计划任务
- 管理员运行：注册 SYSTEM 账户的计划任务

**第四步：注册 NotificationAgent（开机自启）**

创建快捷方式到启动文件夹：

```
目标: pythonw.exe scripts\notify_agent.py
位置: %APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup
```

或者用 PowerShell 创建：

```powershell
$WshShell = New-Object -ComObject WScript.Shell
$shortcut = $WshShell.CreateShortcut(
    "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\MemoryManager-NotifyAgent.lnk"
)
$shortcut.TargetPath = "pythonw.exe"
$shortcut.Arguments = "$env:USERPROFILE\.openclaw\openclaw-memory-manager\scripts\notify_agent.py"
$shortcut.Save()
```

**第五步：验证**

```bash
# 测试流程（不调用模型）
cd scripts
python run_all.py --dry-run

# 手动执行一次完整运行
python run_all.py
```

查看日志：
```bash
type run_log\task-daily-YYYYMMDD.log    # 运行日志
type run_log\diff-YYYYMMDD.log          # 变更 diff（每次进化自动记录）
type run_log\latest_status.txt          # 最新通知状态
```

## 配置说明

### 环境变量

| 变量名 | 必填 | 说明 |
|--------|------|------|
| `MM_LLM_API_KEY` | ✅ | LLM API Key |
| `MM_LLM_PROVIDER` | ❌ | Provider 预设（`deepseek`/`openai`/`moonshot`/`qwen`/`zhipu`/`siliconflow`/`custom`，默认 `deepseek`） |
| `MM_LLM_API_URL` | ❌ | 自定义 API URL（仅 `custom` 或覆盖预设时需要） |
| `MM_LLM_MODEL` | ❌ | 模型名称（覆盖预设时使用） |
| `MM_LLM_TIMEOUT` | ❌ | 请求超时秒数（默认 `90`） |
| `MM_LLM_MAX_RETRIES` | ❌ | 重试次数（默认 `2`） |
| `MM_OPENCLAW_ROOT` | ❌ | OpenClaw 根目录（默认 `~/.openclaw`） |
| `MM_CONVERSATIONS_DIR` | ❌ | 会话记录目录（默认 `~/.openclaw/memory-tdai/conversations`） |
| `MM_WORKSPACE_DIR` | ❌ | workspace 目录（默认 `~/.openclaw/workspace`） |
| `MM_NOTIFY_QUEUE` | ❌ | 通知队列目录（默认 `C:\ProgramData\MemoryManager\notify_queue`） |

> 向后兼容：旧变量名 `MEMORY_MANAGER_DEEPSEEK_API_KEY` 仍生效。

### 自动路径推导

项目放在 `~/.openclaw/openclaw-memory-manager/` 时，脚本自动推导：
- 项目根目录：脚本向上两级（`scripts/common/utils.py` → 项目根）
- OpenClaw 根：`~/.openclaw/`
- 会话记录：`~/.openclaw/memory-tdai/conversations/`
- workspace：`~/.openclaw/workspace/`

如果你的目录结构不同，通过 `MM_*` 环境变量覆盖即可。

## 工程特性

- **原子写入**：先写 `.tmp` 再 `os.replace()`，不会写到一半崩掉
- **每日备份**：`.bak-YYYYMMDD`，同天多次追加序号，自动清理30天前的旧备份
- **Diff 日志**：每次进化自动记录 unified diff，查看 `run_log/diff-YYYYMMDD.log`
- **文件大小限制**：USER.md 5k / MEMORY.md 8k / AGENTS.md 3k，超限自动压缩
- **多 Agent 分区**：按 agent 独立维护记忆（chunking/摘要/进化），main 优先处理
- **进化提示词**：明确三个文件的角色定位和进化方向，AGENTS.md 严格保护原有规则
- **跨文件去重**：同一信息不跨文件重复——用户画像归 USER.md，项目事实归 MEMORY.md，行为规则归 AGENTS.md
- **幂等锁**：PID 检测，防止同一任务重复执行
- **统一调度**：`run_all.py` 确保 daily → weekly → monthly 顺序执行
- **字符限制**：超过 18k 字符自动二次分割，严格控制在 20k 以内
- **容错**：网络异常保留原文件，API Key 双回退（环境变量 + 注册表）
- **`--dry-run`**：跳过所有 LLM 调用，用于验证流程

## 通知机制

```
管理员计划任务 (pythonw.exe)
    │
    └─ send_notification()
        ├─ 写 run_log/latest_status.txt（状态文件）
        └─ 写 notify_queue/*.json
                │
                ▼
NotificationAgent (pythonw.exe, 普通用户, 开机自启)
    │
    ├─ 监视队列目录（2秒轮询）
    ├─ 读取 JSON → WinRT Toast（驻留通知中心）
    └─ 重命名为 .done 后再消费（防重复）
```

为什么需要 NotificationAgent：
- 管理员权限进程的 WinRT Toast 被 Windows 丢弃
- 火绒等安全软件可能拦截 PowerShell 子进程
- Agent 以普通用户权限运行，Toast 正常显示

## 成本明细

以 DeepSeek-v4-flash 为例（`temperature=0.1`）：

| 任务类型 | 月度次数 | 无缓存月费 | 有缓存月费 |
|---------|---------|-----------|-----------|
| 每日 | 30 | 2.25 元 | 1.71 元 |
| 每周 | 4 | 0.40 元 | 0.31 元 |
| 每月 | 1 | 0.13 元 | 0.11 元 |
| **合计** | **35** | **2.78 元** | **2.13 元** |

切换到更便宜的 Provider（如 SiliconFlow）可以进一步降低成本。切换到 GPT-4o-mini 等模型会更贵但效果可能更好。

## 维护

### 更换 API Key

```powershell
[System.Environment]::SetEnvironmentVariable("MM_LLM_API_KEY", "新key", "User")
```

### 更换模型 / Provider

```powershell
# 切换到其他 Provider
[System.Environment]::SetEnvironmentVariable("MM_LLM_PROVIDER", "openai", "User")
[System.Environment]::SetEnvironmentVariable("MM_LLM_API_KEY", "新的key", "User")

# 或者用自定义 Provider
[System.Environment]::SetEnvironmentVariable("MM_LLM_PROVIDER", "custom", "User")
[System.Environment]::SetEnvironmentVariable("MM_LLM_API_URL", "https://api.example.com/v1/chat/completions", "User")
[System.Environment]::SetEnvironmentVariable("MM_LLM_MODEL", "your-model-name", "User")
```

### 回滚进化

```bash
ren USER.md USER.md.failed
ren USER.md.bak-YYYYMMDD USER.md
```

### 查看日志

```bash
type run_log\task-daily-YYYYMMDD.log
type run_log\latest_status.txt
```

### 删除计划任务

```bash
schtasks /Delete /TN "OpenClaw-MemoryManager" /F
```

## 故障排查

### API Key 找不到

计划任务进程可能不继承用户环境变量。解决方案：
1. 设置环境变量后**注销重新登录**（不只是重启终端）
2. 或者同时写入注册表（见"配置 API Key"方式二）

### 通知不弹出

1. 检查 NotificationAgent 是否运行：`tasklist | findstr pythonw`
2. 检查队列目录是否存在：`C:\ProgramData\MemoryManager\notify_queue\`
3. WinRT 依赖是否安装：`pip show winrt-windows.ui.notifications`
4. Agent 以普通用户运行，不要用管理员启动

### 任务被重复执行

检查计划任务是否有多余的重复触发：
```powershell
schtasks /Query /TN "OpenClaw-MemoryManager" /XML | Select-String "Repetition"
```
应无输出。如果有，重新运行 `install_tasks.bat`。

### 进化失败但摘要成功

摘要和进化是两个独立步骤。进化失败时摘要文件仍保留，下次运行会重新进化。

查看失败原因：`type run_log\task-daily-YYYYMMDD.log`，搜索 `ERROR`。

### 文件越来越大

已内置大小限制（USER.md 5k / MEMORY.md 8k / AGENTS.md 3k），超限自动压缩。
如需调整，编辑 `scripts/common/utils.py` 中的 `FILE_SIZE_LIMITS`。

## 许可证

[MIT](LICENSE)
